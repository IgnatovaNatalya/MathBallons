<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_mathballoons"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_mathballoons_round"
        android:supportsRtl="true"
        android:theme="@style/MathBalloonsTheme">

        <service
            android:name="ru.mamsikgames.mathballoons.BackgroundMusic"
            android:enabled="true"
            android:exported="false" />

        <!--activity
            android:name="ru.mamsikgames.mathballoons.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/MathBalloonsSplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-->

        <activity
            android:name="ru.mamsikgames.mathballoons.GameActivity"
            android:exported="true"

            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"

            android:screenOrientation="portrait">


        </activity>
    </application>

</manifest>